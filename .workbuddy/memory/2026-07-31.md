
## M Stand 饮品清单（Excel）
- 来源：桌面截图 PixPin_2026-07-31_09-15-12.png（美团 M Stand 杭州店，520×28717 长图，已切成 14 段存 .workbuddy/crops/）。
- 任务：把饮品按 咖啡/奶茶/果茶 分类整理成 Excel，含价格，排除所有非饮品（蛋糕/手抓饼/咖啡豆/挂耳/包装说明/过敏原提示等），饮品参数用户后续自行补充。
- 交付：`M Stand 饮品清单.xlsx`（openpyxl 生成，.workbuddy/build_excel.py）。列：分类/饮品名称/价格(元)/原价(元)；按分类着色、冻结表头、启用筛选。
- 统计：共 75 款 —— 咖啡 32、奶茶 10、果茶 33。价格为美团到手折扣价（原图 OCR 较长图，价/折可能有偏差，使用前建议核一遍）。
- 注意：当前 data.js 只有 星巴克/瑞幸/喜茶/古茗，未含 M Stand；本清单独立于推荐系统，未改 data.js。
- 原始 `M Stand 饮品清单.xlsx` 被腾讯文档预览打开锁定（生成 `~$M Stand 饮品清单.xlsx`），后续生成均用不同文件名：`(带图)` / `(带图)_v2`。

## M Stand 饮品清单(带图) — 嵌入产品缩略图
- 方法：numpy 行方差法在截图 x=120..345 检测产品行，crop x=120..260 取杯体；合并 gap<20px、min_h>=90；首版得 142 段，后扩到 161 张 .workbuddy/products/item_NNN.png + products_meta.json(y0/y1/h/cy)。
- 交付 v1：`M Stand 饮品清单(带图).xlsx`（build_excel_v2.py）。列：分类/饮品名称/图片/价格(元)/原价(元)；图片缩放到 130px 宽；冻结 A2 + 筛选。共 116 条（咖啡 44 / 奶茶 18 / 果茶 54），首版 9 条无图（标 `[无图-检测漏掉]`）。
- 交付 v2：`M Stand 饮品清单(带图)_v2.xlsx`。手工从桌面原图额外裁出 3 张缺失缩略图（item_162..164），无图条数降到 6。
- 仍无图的 6 条为映射歧义：青芒甘蔗气泡特饮/青芒系列特饮双杯/黑芝麻巴斯克牛乳/无咖牛乳系列双杯套餐/黄油曲奇奶茶/拿铁 —— 经 strip 抽样查看（miss_A2_strip.png / miss_B2_strip.png），多为猜测条目，实际截图对应位置没有同名独立行（如 拿铁 在该区域不可见，黄油曲奇奶茶 实为 黄油鲜牛乳茶），故不强填避免错配。
- 同一饮品多次出现（套餐区与单品区）按截图原样收录，标 `# 重复`，未去重 —— 用户后续自调。

## 沉淀可复用 Skill + 项目约定（用户 10:24 要求）
- 用户要求把"菜单截图→含图 Excel"流程记住，后续给别的图片按相同步骤生成；并明确新规：**过滤非饮品 + 套餐，价格只展示原价(元)**。
- 已创建用户级 skill：`menu-drinks-to-excel`（~/.workbuddy/skills/），含 scripts/extract_products.py（numpy 行方差检测+裁杯体）、scripts/build_excel.py（由 ITEMS 生成 分类/饮品名称/图片/原价(元) 含图 Excel）、references/workflow.md（端到端步骤+分类关键词+过滤规则+坑）。已通过 package_skill 校验。
- 已写项目 MEMORY.md：固化"每版统一规则 + 运行环境 + 无法读图靠 OCR/让用户补清单"等约定。
- 注意：现有 M Stand 带图文件按老规则（含套餐、价+原价两列）生成，与新规不一致；若要与新规则统一需重做。

## M Stand 杭州星光大道店（新规重做）
- 来源：桌面截图 `MSTAND.png`（美团 M Stand 杭州滨江星光大道店外卖门店，521×14480）。
- 用户要求："按之前流程生成"（即用新规：过滤非饮品+套餐、只展示原价、按咖啡/奶茶/果茶 分类）。
- 自动检测：脚本 extract_products.py 得 71 个产品行 → .workbuddy/mstand_products/item_001..071.png + .workbuddy/mstand_meta.json。
- 识别：rapidocr 3.8.1 OCR 截图（系统 Python 3.10 自带）。对缺价/缺标题的 region（OCR 在大杯段边缘漏掉了价格）做了手动复核 —— .workbuddy/look_*.png / big_item_*.png 逐项核对。
- 分类规则：美式/冷萃/拿铁（plain 或仅 flavor）/澳白/卡布/拼配/黑咖 → 咖啡；抹茶*/紫芋*/巴斯克*/芝士云朵/牛乳/鲜乳/白月光/椰青/椰子水/奶盖 → 奶茶；茶/气泡饮/纯果汁/油柑/杨桃/杨梅/葡萄/梅/柠/黄杏/青麦草/话梅/山竹 → 果茶。
- 过滤：跳过 idx 29（季节新豆双杯套餐）、idx 34, 35（咖啡豆，非饮品）。去重：idx 3=5, 4=6, 1=48, 2=56 相同 (title, price) 只保留首条。
- 交付：`M Stand 杭州星光大道店(带图).xlsx`，64 款（咖啡 35 / 奶茶 13 / 果茶 16），列：分类 | 饮品名称 | 图片 | 原价(元)，全部含图（0 无图），按分类着色、冻结 A2 + 筛选。脚本：`.workbuddy/ITEMS.py` + skill 的 build_excel.py。
- 旁注：用户 M Stand 菜单**有两个价位段**（超大杯 ¥30-45、标准杯 ¥28-38），同一饮品不同 size 视为不同 SKU 全部保留；同名同价（在不同区块重复出现）视为重复 SKU，去重。

## 修正"饮品居中"问题（用户 14:22 反馈）
- 用户反馈原 _v2 缩略图"饮品不在正中、左边白边多"。根因：旧 `center_on_canvas` 用"非白底整体"取包围盒，把杯子旁边**浅灰投影**也算进内容，包围盒被拉偏，杯子本体偏到一侧。
- 修复：改为用"有色彩 或 明显偏暗"的像素（杯子实体：液体/有色盖子/杯身轮廓/咖啡）取包围盒，排除浅灰投影；固定画布 260×220（=Excel 130×110 单元格 2 倍，比例一致，缩放后正好填满不溢出）。crops 用 crop_x0=130, crop_x1=250 夹住杯体、避开左侧分类徽章与右侧文字。
- 已同步更新 skill 的 extract_products.py + references/workflow.md。
- 重交付：`M Stand 杭州星光大道店(带图)_v3.xlsx`，64 款全含图；全量校验 71 张杯子实体水平偏移均值 +1.5px、无 >15px 的 outlier（唯一 -28px 是已过滤的咖啡豆 item_035，不进清单）。

## STARBUCKS 杭州星飞路店（按之前流程生成）
- 来源：桌面 `STARBUCKS.png`（美团星巴克专星送，521×16560）。布局：左侧 x 0-100 是类目导航，中间 x 120-260 是杯体，右侧 280+ 是名称/价格。
- 检测：默认 thr=25 只能切 71 行且合并严重（6 段含 2-3 款），给脚本加 `--thr`/`--min-h` CLI 参数后用 thr=47, min_h=80 切出 79 行，全部干净不合并。已更新 skill 脚本。
- 识别：rapidocr OCR 右侧文字（x 280-521）。OCR 有不少异常：标题取错（选了描述行）、缺价（idx 3/8/14/19/25/31/39/50）、分类错（莓莓*被误分果茶）—— 全部按视觉复核校正后写入 `.workbuddy/sb_ITEMS.py`。
- 分类规则要点：含 **拿铁/美式/馥芮白/冷萃/浓缩/鸳鸯/玛奇朵/摩卡/海盐/玫瑰/燕麦/椰香/意式/椰子丝绒/柠柚/风味美式** 任意之一 → 咖啡；含 **星冰乐/可可/摩卡/巧克力/芝士/奶/抹茶/焙茶/燕麦/椰/厚乳/奶油/风味饮/蒸气奶/薄荷冰巧/巧克力饮品/红茶拿铁/抹茶拿铁/焙茶拿铁** → 奶茶；含 **冰摇/冰茶/果茶/芒/西番莲/莓莓/石榴/茉吉托/映夏/红莓/黑加仑** → 果茶。
- 过滤/去重：OCR 重复的（巴西莓果昔、石榴爆珠、抽柚茉莉、拿铁、美式、蒸气奶、高蛋白等）只保留首条。
- 交付：`STARBUCKS 杭州星飞路店(带图).xlsx`，67 款（咖啡 36 / 奶茶 24 / 果茶 7），全含图，2.8MB。全量居中校验：79 张均值 -0.9px，±4px 内（星巴克杯子色块对比强，比 M Stand 投影还好处理）。

## 把 Excel 真实 SKU 导入 data.js（推荐系统，16:11）
- 用户给两份已生成的 Excel：`Codex/2026-07-31/ban/outputs/STARBUCKS_饮品SKU清单.xlsx`（83 行：咖啡49/奶茶22/果茶12）、`MSTAND_饮品SKU清单.xlsx`（61 行：咖啡36/奶茶5/果茶20）。列：类别/名称/图片/单价（元）；每杯一张内嵌图（oneCellAnchor，row 0-based+1=openpyxl 行，rId{k}→media/image{k}.jpeg，1:1）。
- 用系统 Python 3.10（自带 openpyxl 3.1.5 + PIL 11.1）写 `gen_data.py`：解析 drawing 锚点把每张图按行号映射到 SKU，图片用 PIL 重编码（最大边 600px、JPEG q85）存入 `images/starbucks/{行号}.jpg`、`images/mstand/{行号}.jpg`；生成 `data.js`（json 格式，含顶部注释）。
- 映射结果：星巴克 83 款替换原占位（分布 coffee49/milktea22/fruittea12）；**新增 M Stand 品牌** 61 款（coffee36/milktea5/fruittea20）；保留 瑞幸/喜茶/古茗 占位。共 144 款真实 SKU 全部带图，0 缺失文件。
- 校验：144 个 img 路径全部存在；node 校验 data.js 语法 OK；线上 `data.js` 含 `"mstand"` 且 144 条 img，图片 `images/starbucks/2.jpg` 与 `images/mstand/3.jpg` 均 200。
- 注意：腾讯文档连接器本会话断开，按路由规则回退本地 Python 处理（已告知用户）。本机模型无法读图，图片与 SKU 的对应靠 Excel 锚点结构保证，未逐张肉眼核对。
- 已提交并 push（0a39efb..736ae5f），GitHub Pages 自动重建；线上链接不变：https://hellocyyyy.github.io/drink-recommend/ 。`gen_data.py` 保留以备重新生成。
