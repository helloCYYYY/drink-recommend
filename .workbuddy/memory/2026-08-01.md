# 2026-08-01

## 重录星巴克饮品（图片居中版）
- 用户反馈之前星巴克图片有问题，给了新版 `STARBUCKS_饮品SKU清单_饮品居中版.xlsx`（"饮品居中版"，图片已居中修正）。
- 改 `gen_data.py`：STARBUCKS 源路径指向新版居中 Excel；锚点正则加固为 `<from>.*?<col>(\d+)</col>.*?<row>(\d+)</row>`（不再强依赖 `<colOff>`，换文件也不会抓不到图）。
- 重跑 `gen_data.py`：仅覆盖 `images/starbucks/` 的 83 张图（文件名 2.jpg…84.jpg 不变，内容替换为居中修正版），重新生成 `data.js`。
- 数量核对：星巴克 49+22+12=83、M Stand 36+5+20=61，均无丢失；M Stand 与瑞幸/喜茶/古茗占位品牌未动。
- 提交并 push 到 GitHub Pages（https://hellocyyyy.github.io/drink-recommend/）。
- 注：源图尺寸仅 100×130，索引页用 object-fit:cover 塞 46/96px 圆角框，竖图居中后才不会被上下裁切——正是"居中版"修复的点。

## 补充瑞幸咖啡真实清单
- 新源 `瑞幸咖啡_饮品SKU清单_补全版.xlsx`（4列：类别/名称/图片/单价，与星巴克模板一致），74款（咖啡40/奶茶19/果茶15），74张图。
- `gen_data.py`：新增 LUCKIN 提取；`extract()` 改为按 brand_key 动态建 `images/<品牌>/` 目录（之前只预建 starbucks/mstand，导致 luckin 报 FileNotFoundError）；合并纳入 `lk`，并删除占位里的旧瑞幸假数据（生椰拿铁等）。
- 真实清单无"生椰拿铁"，定制参数面板需样本 → 把演示 opts(温度/甜度/加料)挂到真实存在的"生椰丝绒拿铁"上。
- data.js 现：星巴克83 / M Stand61 / 瑞幸74 / 喜茶7 / 古茗7。已 push 上线。

## 古茗截图踩坑（图片过窄，OCR 全乱）
- 用户发来 `古茗.png` 想做古茗清单。但 `file` 出来 `77x1920, baseline` —— **实际像素宽只有 77px**，被严重横向压缩。
- rapidocr ch_PP-OCRv4 在 800w 拉宽版上跑 10s 出 75 个文本框，**中文 95% 乱码**，仅价格数字和一些英文/数字勉强能读。
- 靠肉眼逐 33px 行拼图（拉宽 12x）Read 了 5 批 59 行 + 600w 拉宽版 15 段，能 100% 确认的单品约 15 款（白兰碧螺春/白兰鲜椰乳/古茗·翠嘟嘟/茉莉鲜芒果/鲜爽金芒果/鲜爽金柚/霸气花果茶/霸气葡萄果茶/鲜爽桃桃/麦乐鲜果/荔芒鲜果/百香鲜果 等），其余模糊。
- 用户的回复是"重新截图（推荐）"——等用户重发清晰图后按之前流程跑。
- 关键经验写入 MEMORY.md 和 skill workflow.md：**收到新图先 `file` / `PIL.size` 确认宽高**，< 300px 宽立刻提示重截，不要硬撑。

## 更新古茗真实清单（用 Excel 而非截图）
- 用户纠正：之前 `古茗.png` 截图太窄作废，真正源是 `古茗_饮品SKU清单.xlsx`（同星巴克/瑞幸模板：类别/名称/图片/单价col4），47款（咖啡10/奶茶22/果茶15），47张图。无套餐。
- `gen_data.py`：新增 GUMING 提取（gm）；合并循环从 `(sb,ms,lk)` 扩为 `(sb,ms,lk,gm)`；删除 placeholders 里 milktea/fruittea 两处古茗假占位（烤奶/超A芝士葡萄/杨枝甘露/百香果双响炮等）。
- 验证：images/guming 47张、data.js 引用47条；data.js 中"古茗奶茶"是 Excel row42 真实款(price10)，非占位残留。
- 演示 opts 样本仍只在瑞幸"生椰丝绒拿铁"（古茗无需再挂）。
- data.js 现：星巴克83 / M Stand61 / 瑞幸74 / 古茗47 / 喜茶7。已 push 上线。

## 补充OT另茶真实清单
- 新源 `OT另茶_饮品SKU清单.xlsx`（同模板：类别/名称/图片/单价col4），23款（果茶14/奶茶8/咖啡1），23张图。无套餐。
- `gen_data.py`：新增 otlc 提取（品牌key `otlc`，display「OT另茶」，logo 🍃）；合并循环从 `(sb,ms,lk,gm)` 扩为 `(sb,ms,lk,gm,ot)` 五表。OT另茶为新品牌，无旧占位，直接并入。
- data.js 现：星巴克83 / M Stand61 / 瑞幸74 / 古茗47 / OT另茶23 / 喜茶7 = 共 295 款。已 push 上线。

## 补充茉莉奶白真实清单
- 新源 `茉莉奶白_饮品SKU清单.xlsx`（同模板），37款（果茶17/奶茶20，无咖啡），37张图。无套餐。
- `gen_data.py`：新增 molly 提取（品牌key `molly`，display「茉莉奶白」，logo 🌿）；合并循环扩为 `(sb,ms,lk,gm,ot,molly)` 六表。茉莉奶白为新品牌，无旧占位，直接并入。
- data.js 现：星巴克83 / M Stand61 / 瑞幸74 / 古茗47 / OT另茶23 / 茉莉奶白37 / 喜茶7 = 共 332 款。已 push 上线。

## 补充裕莲茶楼真实清单
- 新源 `裕莲茶楼_饮品SKU清单.xlsx`（同模板），31款（奶茶12/果茶17/咖啡2），31张图。无套餐。类别全被识别（咖啡/奶茶/果茶）。
- `gen_data.py`：新增 yulian 提取（品牌key `yulian`，display「裕莲茶楼」，logo 🏮）；合并循环扩为七表。裕莲茶楼为新品牌，无旧占位，直接并入。
- data.js 现：星巴克83 / M Stand61 / 瑞幸74 / 古茗47 / OT另茶23 / 茉莉奶白37 / 裕莲茶楼31 / 喜茶7 = 共 363 款。已 push 上线。

## 补充 KOI 真实清单
- 新源 `KOI_饮品SKU清单.xlsx`（同模板），42款（奶茶27/果茶15，无咖啡），42张图。无套餐，类别全被识别。复用 `beverage-excel-to-datajs` skill 流程。
- `gen_data.py`：新增 koi 提取（品牌key `koi`，display「KOI」，logo 🧋）；合并循环扩为八表。KOI 为新品牌，无旧占位，直接并入。
- data.js 现：星巴克83 / M Stand61 / 瑞幸74 / 古茗47 / OT另茶23 / 茉莉奶白37 / 裕莲茶楼31 / KOI42 / 喜茶7 = 共 405 款。已 push 上线。

## 补充喜茶真实清单（替换占位）
- 新源 `Documents/Codex/2026-08-01/new-chat/outputs/喜茶饮品清单.xlsx`（同模板，路径在 new-chat 而非 ban），35款（奶茶9/果茶26，无咖啡），35张图。无套餐，类别全被识别。复用 `beverage-excel-to-datajs` skill。
- `gen_data.py`：新增 hytea 提取（品牌key `heytea`，display「喜茶」，logo 🟣，与占位同 key）；**删除原 placeholders 字典（喜茶7款假占位：烤黑糖波波牛乳/芝芝莓莓/多肉葡萄等空img）**；合并循环扩为九表 `(sb,ms,lk,gm,ot,molly,yulian,koi,hytea)`，去掉占位合并分支。
- 验证：data.js 喜茶引用35、旧占位名已不在（grep「烤黑糖波波牛乳」命中的是真实「…牛乳茶/牛乳(首创)」子串，非旧占位）。
- data.js 现：星巴克83 / M Stand61 / 瑞幸74 / 古茗47 / OT另茶23 / 茉莉奶白37 / 裕莲茶楼31 / KOI42 / 喜茶35 = 共 433 款。已 push 上线。
