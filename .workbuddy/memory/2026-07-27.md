# 2026-07-27

## 饮料推荐系统（给女朋友用）

- 需求：给女朋友用的饮料推荐系统，iPhone 16 Pro 用户。支持选大类（咖啡/奶茶/果茶）→ 品牌（星巴克/瑞幸/喜茶/古茗）→ 具体 SKU 三级联动，并支持「随机惊喜」功能。
- 决策：先做成手机网页（响应式 H5），暂不做小程序——迭代快、零审核、可发链接。后续如需微信入口/订阅提醒再包小程序，核心逻辑可复用。
- 已交付：`index.html` 单文件原型（含 iPhone 16 Pro 设备外壳预览 + 真机满屏自适应）。
- 数据外置：把 SKU/价格/标签抽到独立 `data.js`，用户以后只改 `data.js` 即可更新全部真实内容，无需碰逻辑代码（文件内已加中文注释说明结构）。
- 数据外置：把 SKU/价格/标签抽到独立 `data.js`，用户以后只改 `data.js` 即可更新全部真实内容，无需碰逻辑代码（文件内已加中文注释说明结构）。
- UI 改色：整体已改为蓝色系（变量 --rose=#3b82f6 / --rose-deep=#2563eb / --peach=#7dd3fc，背景与阴影同步改蓝）。
- 部署坑（重要）：CloudStudio 沙箱服务会被回收、接口还连续 400/JSON 故障；Cloudflare Pages 的 `*.workers.dev` 在国内（尤其手机流量）被墙/被拦，打不开。最终可用方案 = **GitHub Pages**。
- 部署（最终可用）：GitHub 仓库 https://github.com/helloCYYYY/drink-recommend （本地已 git init 并提交；仓库已设为 public 才能开 Pages；remote 已清理 token）。已通过 GitHub API 开启 Pages（source: master /）。
  - **正式线上链接（已验证：手机流量可打开 + 200 + 蓝色主题 + 无粉色残留）：https://hellocyyyy.github.io/drink-recommend/**
  - 备用（国内不稳，已弃用）：Cloudflare https://drink-recommend.btcyhello.workers.dev/ （国内打不开）
  - 更新流程：本地改完 `data.js` → `git add data.js && git commit -m "..." && git push` → GitHub Pages 自动重新部署，链接不变。
  - 注：GitHub token 用过一次后建议去 settings/tokens 撤销。旧的 `饮料推荐系统_v2` 为临时副本，可忽略/删除。
- 待办：用户后续替换真实 SKU 数据；2/3 功能（偏好权重、心情天气维度）暂不做。
